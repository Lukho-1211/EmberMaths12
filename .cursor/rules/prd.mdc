---
description: Ember Maths12 product requirements — read docs/PRD.md for feature work
alwaysApply: true
---

# Product requirements

South African CAPS Grade 12 Mathematics school: portals `/admin`, `/student`, `/teacher`, `/parent`.

**Before product, UX, routing, or feature work, read [`docs/PRD.md`](docs/PRD.md).** Treat it as product law. Stack/auth/UI conventions stay in `ember12.mdc`.

## Non-negotiables

- Authorization role from `profiles` only — never `user_metadata`
- Admin role assignment requires Auth `app_metadata.role = admin`; never trust `user_metadata` for admin
- No `localStorage` app-state persistence; write through Supabase
- Student progress and corrections: server API + RPC only — no direct client upserts
- Default assessment pass mark **50%**; past papers do **not** affect pass/fail
- Day lesson complete is gated on passing `lessonTest` when one exists

## MUST NOT build (v1)

- Live video hosting
- Email confirmation productization
- Payments / billing

**L2 is now** (`docs/PRD.md` §5.3 / §8): replace `mockPaperGrade` and mock MCQ generation. First slice is Saturday week-test scan marked against the admin memo. Grading stays on the server (API + RPC); past papers stay practice-only.

## Gaps — do not invent unless tasked

Parent–child link UI (seed/SQL only — no linking UI anywhere), student class-join UI, student study groups, teacher lessons in student Learn, badge copy vs earn-logic mismatch.

## Later is not now

Items marked `later` in `docs/PRD.md` stay unimplemented unless the task explicitly promotes them.

If a request conflicts with the PRD, **follow the PRD** and say so.
